soulseek:
  # Credentials will be set via environment variables
  # Set SLSKD_SOULSEEK_USERNAME and SLSKD_SOULSEEK_PASSWORD in .env file
  description: antifa
  # address: vps.slsknet.org
  # port: 2271
  address: server.slsknet.org
  port: 2242
  
web:
  url_base: /slskd
  # Authentication will be set via environment variables
  # Set SLSKD_WEB_USERNAME and SLSKD_WEB_PASSWORD in .env file
    
shares:
  directories:
    - /music
    - /downloads
  cache:
    storage_mode: disk
    workers: 4
    retention: 10080 # 1 week
    
directories:
  # incomplete: /incomplete
  # downloads: /downloads

global:
  upload:

logging:
  level: Info
  
remote_configuration: true
